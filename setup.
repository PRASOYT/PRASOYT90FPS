apt update
apt install toilet

pkg install libandroid-wordexp

pkg install zip
pkg install unzip



if [ -f $HOME/FPSTOOL/FPSTOOL ]; then
chmod +x $HOME/FPSTOOL/FPSTOOL
mv $HOME/FPSTOOL/FPSTOOL $PREFIX/bin
fi

chmod +x 90FPS.sh
./90FPS.sh
